/**
 * 业务逻辑处理层（Service）
 */

import { Injectable } from '@nestjs/common';

import { Create{{ upperName }}DTO, Find{{ upperName }}DTO, Paginated{{ upperName }}DTO, Update{{ upperName }}DTO } from '../dto';
import { {{ upperName }}Provider } from '../provider';

@Injectable()
export class {{ upperName }}Service {
  constructor(
    private readonly {{ fileName }}Provider: {{ upperName }}Provider,
  ) {}

  /** 单条查询 */
  public async findOne(dto: Find{{ upperName }}DTO) {
    return this.{{ fileName }}Provider.findOne(dto);
  }

  /** 多条查询 */
  public async findAll(dto: Find{{ upperName }}DTO) {
    return this.{{ fileName }}Provider.findAll(dto);
  }

  /** 分页查询 */
  public async paginated(dto: Paginated{{ upperName }}DTO) {
    return this.{{ fileName }}Provider.paginated(dto);
  }

  /** 创建 */
  public async create(dto: Create{{ upperName }}DTO) {
    return this.{{ fileName }}Provider.create(dto);
  }

  /** 更新 */
  public async update(id: string, dto: Update{{ upperName }}DTO) {
    return this.{{ fileName }}Provider.update(id, dto);
  }

  /** 按主键ID删除 */
  public async delete(id: string) {
    return this.{{ fileName }}Provider.delete(id);
  }
}